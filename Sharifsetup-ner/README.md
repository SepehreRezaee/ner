Place the offline Hugging Face snapshot for `fastino/gliner2-multi-v1` in this folder.

Run:

```bash
python3 scripts/download_model.py
```

The Docker build expects `Sharifsetup-ner/config.json` to exist.
